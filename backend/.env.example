# Authentication Configuration
# JWT secret key - must be at least 32 characters, use a strong random string
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-here-minimum-32-characters-long

# JWT algorithm (HS256 or RS256)
JWT_ALGORITHM=HS256

# Token expiration time in hours
JWT_ACCESS_TOKEN_EXPIRE_HOURS=24

# Password hashing rounds (bcrypt cost factor)
BCRYPT_ROUNDS=12

# Application Configuration
# API version (used in OpenAPI docs)
API_VERSION=1.0.0

# API title (used in OpenAPI docs)
API_TITLE=Task Tracker API

# API author/contact name (used in OpenAPI docs)
API_AUTHOR=Michael Wybraniec

# API description (used in OpenAPI docs, can be multi-line with \n)
API_DESCRIPTION=Task Tracker API - Full-stack task management application.\n\n**Built with Agentic Workflow Protocol (AWP)** - See `agentic-sdlc/AWP.md` for structured development workflow.\n\n**Author:** Michael Wybraniec\n\n**Features:** JWT authentication, task management, file attachments, search & filtering, notifications, audit trail.
